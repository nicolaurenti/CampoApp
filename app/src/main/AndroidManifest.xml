<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.campoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_esquina_de_campo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_esquina_de_campo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name="com.example.campoapp.presentation.mvp.view.activities.InformationActivity" />
        <activity
            android:name="com.example.campoapp.presentation.mvp.view.activities.ClientActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>